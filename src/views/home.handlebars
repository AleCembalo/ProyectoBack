<div>
    <h1>Lista de productos</h1>

    <ul class="list-group">
        {{#each products}}
        <li class="list-group-item">{{title}}, ($ {{price}}), (Stock: {{stock}})</li>
        {{/each}}
    </ul>
</div>